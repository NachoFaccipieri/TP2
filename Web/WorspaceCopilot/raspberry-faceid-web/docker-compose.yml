version: "3.8"

services:
  backend:
    build:
      context: ./server
    container_name: faceid-backend
    # importante: usar la red del host para acceder a mosquitto en localhost
    network_mode: "host"
    restart: unless-stopped
    volumes:
      - ./server:/app
    environment:
      - FLASK_ENV=production